diagnostic: align MPO numbers with Census productive set (epoch 623)

The pools-distribution doc and parent README quoted MPO totals computed
from a koios snapshot with no productive filter (85 entities / 901 pools
/ 16.4B ADA / 75.4%). Census's canonical pass-3 figures, derived at
epoch 623 with the productive filter (>=1M ADA per pool), give 83
entities / 475 pools / 16.29B ADA / 75.5% of productive stake. The two
counts diverge because the koios snapshot is older and includes
sub-1M-ADA pools.

Switched the MPO-counting scripts to source pool stake from
pool_stake_623.csv with the >=1M productive filter, matching Census
build_entity_tier_crossref.py exactly:
  - build_mpo_taxonomy_by_stance_visual.py (pools-distribution)
  - build_mpo_extraction_visual.py        (pools-distribution)
  - build_mpo_tier_stance_entity_visual.py (census)
Pledge data still comes from koios_pool_list_mainnet.csv since pledge is
a registration parameter that doesn't drift epoch-to-epoch.
Denominators switched from total-active-stake (21.75B) to productive
total (21.57B) to match Census's "% of productive stake" framing.
Figures regenerated.

Aligned headline numbers across the diagnostic, V2 spec README,
solution-evaluation, CPS, and findings docs:
  85 entities -> 83 attributed entities (73 strict multi-pool + 10
                attributed single-pool)
  901 pools  -> 475 productive pools
  16.4B      -> 16.29B ADA
  75.4% of staked supply -> 75.5% of productive stake
  37 sub-saturation -> 35
  41 of 48 saturation-scale non-compliant -> 42 of 48
  78 of 85 outside pledge-response path -> 77 of 83
  13.74B (63%) outside path -> 13.93B (64.6% of productive)
  283 independent viable operators -> 284
  CEX+IVaaS 7.4B (19.2% of supply) -> 7.41B (34.3% of productive)
  3 exemplary entities -> 2 (CHUCK BUX falls to 75.9% at epoch 623,
                              moving from exemplary to compliant)

POL.O5 cell title rewritten ("83 multi-pool operators control 75.5%
of productive stake"), POL.O5.F1-F7 findings rewritten, POL.O7 cell +
F1 updated, POL.O6 narrative tweaked to 284. Body prose at sections
4.2.1 / 4.2.2 / 4.2.4 / 4.3 / 4.4 reflowed. Reward-loss table at
4.2.4.3 marked as a structural illustration (rounded estimates) since
the underlying reward-anatomy script isn't re-run here. Top-10 most-
exemplary MPOs table replaced with epoch-623 entity-level pledge
ratios.

References to 85/901 in nakamoto-revaluation §"Data provenance" and
census F9 / entity-lifecycle "42 of 85" preserved -- those describe
the raw attribution mapping (902 rows, 85 distinct entities) or the
full lifecycle universe before the productive filter, not the
productive subset.
