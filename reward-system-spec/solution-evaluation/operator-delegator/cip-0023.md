# CIP-0023 — Fair Min Fees (`minPoolMargin`)

CIP-0023 introduces a single new parameter — **`minPoolMargin`**, a floor on the variable-fee margin that operators charge delegators. It is a fee-layer instrument: the V1 reward formula and per-pool allocation are left untouched; only the operator/member split *after* the allocation changes. The proposal is the historical anchor for the margin-floor concept and predates the broader [CIP-0082](cip-0082.md) reform.

The intent is to make small pools more **ROS-attractive** to delegators by setting a floor under operator margin — preventing a race-to-zero where small operators undercut each other on margin to the point that none of them can cover their fixed cost. As an instrument it is conservative: one new scalar parameter, no hard fork, no re-registration, no change to existing pool certificates.

The catch is structural. CIP-0082 stage 2 reproduces the same intent inside a larger staged package (with a rate-floor primitive that subsumes the margin-floor mechanic). As a live candidate today, CIP-0023 is **dominated by CIP-0082 stage 2** unless governance explicitly declines the hard fork that stage 2 requires.

*The core question this evaluation asks: under what conditions does CIP-0023 add value beyond CIP-0082, and under what conditions does it introduce regressive transfers up the distribution?*

The argument proceeds in three parts:

1. **Introduction** (§1). CIP identity card, origin (2021 — Lujhön Sotelo), the diagnostic findings around small-operator viability and ROS convergence the proposal targets, and the V2 §3 problem statement (Missing CPS) it implicitly addresses.

2. **Mechanism** (§2). The fee-split formula with the new `minPoolMargin` floor, worked calibration at current mainnet parameters, the regime where the floor binds, and the per-pool effects across the canonical 9-tier taxonomy.

3. **Limits as a standalone proposal** (§3). Three synthesis findings — S1 (functional redundancy with CIP-0082 stage 2 — Regresses), S2 (partial CPS coverage with regressive leakage up the distribution — Regresses), S3 (blind spot at the below-floor tail where the instrument does not reach the population it is meant to help — Blind spot).

The Executive summary below packages the verdict; the Findings summary table after it is the navigation index for §3.

## Executive summary

- **Verdict.** **Not recommended as a standalone proposal to ratify.** CIP-0023 is a historical reference point for the margin-floor concept; as a live item in the current review cycle it is dominated by CIP-0082 stage 2 (which reproduces the same instrument inside a larger staged package) and it addresses only half of the reconstructed CPS (§1.4) — ROS convergence across pool sizes — while leaking operator-side revenue to pools that have no viability problem.
- **Instrument.** One new parameter, `minPoolMargin` (variable-fee floor), applied inside the ledger's reward calculation after `minPoolCost`. No hard fork, no re-registration.
- **Three synthesis findings carry the verdict** — detailed in the next section and cross-referenced throughout:
  - **S1** — Functionally redundant with CIP-0082 stage 2; no additional mechanism, doubled governance surface (1 F: §3.1).
  - **S2** — Partial CPS coverage: the margin floor leaks revenue up the distribution — regressively on both the pool axis and the operator-fleet axis (3 F's: §3.2).
  - **S3** — Blind spot at the below-floor tail: Dormant and Sub-production tiers stay 100 % fee-consumed; the instrument does not reach the population it is nominally meant to help (1 F: §2.2).

## Findings summary

CIP evaluations use a three-level taxonomy mirroring the diagnostic's `SUBFLOW.O<n>.F<m>`: **S<n>** = synthesis-level verdict statement; **F<m>** = quantified finding anchored in §2 / §3 evidence; each F tagged **[D] Delivers / [R] Regresses / [B] Blind spot**. Each finding is re-surfaced as an inline callout where it emerges in the body of the document — the table below is a compact index.

| Code | Tag | Claim | Emerges in |
|---|---|---|---|
| **CIP-0023.S1** | | **Functionally redundant with CIP-0082 stage 2** | §3.1 |
| CIP-0023.S1.F1 | [R] | `minPoolMargin` (CIP-0023) is mechanically identical to `minPoolRate` (CIP-0082 stage 2); CIP-0082 author explicitly credits CIP-0023. Enacting both doubles the governance surface without adding mechanism | §3.1 scenario table |
| **CIP-0023.S2** | | **Partial CPS coverage: the margin floor leaks revenue up the distribution — regressively on both the pool axis and the operator-fleet axis** | §3.2 |
| CIP-0023.S2.F1 | [D] | Standalone variant compresses productive-range delegator fee-rate dispersion **38× → 13×**; partial delivery on the §3.3 delegator-yield claim | §2.2 effective-fee-rate table |
| CIP-0023.S2.F2 | [R] | Per-pool Δ at Saturation is **52×** the Δ at Sub-viable (+357.5 vs +6.8 ₳/ep); Dormant / Sub-production receive **zero** uplift | §3.2.1 nine-tier table |
| CIP-0023.S2.F3 | [R] | On the n-MPO fleet axis the transfer compounds: 11+ pool MPO gains **+216 K ₳/yr** vs sub-viable single-pool **+431 ₳/yr** — a **502× gap**; two amplifications (pool-axis 52× × fleet-axis 9.6×) stack | §3.2.2 n-MPO table |
| **CIP-0023.S3** | | **Blind spot at the below-floor tail — the instrument does not reach the struggling population it is meant to help** | §2.2 |
| CIP-0023.S3.F1 | [B] | Dormant and Sub-production tiers remain **100 % fee-consumed** today and under the standalone variant; even the out-of-scope paired variant leaves Dormant untouched | §2.2 fee-rate table |

*Cross-CIP link.* **CIP-0023.S2** is the smaller-scale version of [CIP-0082.S1](cip-0082.md#findings-summary). CIP-0082's Margin swap scales the same transfer up **~2× (pool axis) / ~4× (fleet axis)** by pushing `minPoolCost → 0` and `minPoolMargin → 3 %`.

## Table of Contents

- [Executive summary](#executive-summary)
- [Findings summary](#findings-summary)
- [1. Introduction](#1-introduction)
  - [1.1 Identity card](#11-identity-card)
  - [1.2 Origin and context](#12-origin-and-context)
  - [1.3 Related diagnostic findings](#13-related-diagnostic-findings)
  - [1.4 Missing CPS — mapping to V2 §3.1](#14-missing-cps-mapping-to-v2-31)
- [2. Mechanism](#2-mechanism)
  - [2.1 Worked calibration from the CIP rationale](#21-worked-calibration-from-the-cip-rationale)
  - [2.2 Updated calibration at current parameters (epoch ~623, 2026/04)](#22-updated-calibration-at-current-parameters-epoch-623-202604)
- [3. Limits of CIP-0023 as a standalone proposal](#3-limits-of-cip-0023-as-a-standalone-proposal)
  - [3.1 Functional redundancy — CIP-0082 stage 2 reproduces the same instrument](#31-functional-redundancy-cip-0082-stage-2-reproduces-the-same-instrument)
  - [3.2 Partial coverage of the missing CPS — the instrument leaks revenue toward pools with no viability problem](#32-partial-coverage-of-the-missing-cps-the-instrument-leaks-revenue-toward-pools-with-no-viability-problem)
    - [3.2.1 Pool axis — Δ per pool across the nine canonical tiers](#321-pool-axis-per-pool-across-the-nine-canonical-tiers)
    - [3.2.2 Operator axis — Δ per entity by n-MPO bracket](#322-operator-axis-per-entity-by-n-mpo-bracket)
    - [3.2.3 Reading — why this fails the missing CPS](#323-reading-why-this-fails-the-missing-cps)
  - [3.3 References](#33-references)

## 1. Introduction

**CIP-0023 — "Fair Min Fees".** A 2021 proposal to introduce a floor on the variable-fee component of the operator take (`minPoolMargin`), targeting the regressive effect of the existing fixed pool cost on delegators to small pools.

### 1.1 Identity card

| Field | Value |
| --- | --- |
| CIP number | CIP-0023 |
| Title | Fair Min Fees |
| Authors | Shawn McMurdo, Ryan Wiley |
| Created | 2021/02/04 |
| Category | Ledger |
| Status | Proposed (as of 2026/04) |
| Official page | [cips.cardano.org/cip/CIP-0023](https://cips.cardano.org/cip/CIP-0023) |
| Source (GitHub) | [cardano-foundation/CIPs / CIP-0023](https://github.com/cardano-foundation/CIPs/tree/master/CIP-0023) |
| Discussion PR | [cardano-foundation/CIPs #66](https://github.com/cardano-foundation/CIPs/pull/66) |
| Forum thread | [forum.cardano.org/t/fair-min-fees-cip/47534](https://forum.cardano.org/t/fair-min-fees-cip/47534) |

### 1.2 Origin and context

**Authorship and moment.** Written in early-Shelley era, February 2021. The worked example in the CIP uses epoch-382 parameters, with `minPoolCost` at 340 ADA and `a0` at 0.3. The authors' observation: a 100 k ADA delegator faced an effective fee rate of **22.7 %** in a 2 M ADA pool versus **0.7 %** in a saturated 64 M ADA pool — a 32× dispersion driven almost entirely by the fixed cost component. The CIP framed this as a *fairness* problem — delegators penalised for spreading stake across smaller productive pools, and small operators structurally disadvantaged.

**Scope the CIP explicitly excludes.** The text is explicit: *"this proposal does not change or reduce the existing minimum fixed pool fee."* The CIP proposes the margin-floor instrument only. Any reduction in `minPoolCost` is left to a separate governance action. This self-imposed scope restriction is load-bearing for the evaluation — it is why the paired variant in §2.2 and §3.2 is labelled out-of-scope for the standalone CIP.

**Historical placement.** Written before Conway-era governance and before CIP-0082 — whose stage 2 reproduces the margin-floor mechanism under a different name (`minPoolRate`). CIP-0023 is therefore both a live technical proposal and a historical reference point for the margin-floor concept that CIP-0082 later operationalises within a staged path. §3.1 resolves the current relationship with CIP-0082.

### 1.3 Related diagnostic findings

CIP-0023 is a fee-layer instrument acting on the operator/delegator split *after* per-pool allocation. Its rationale attacks two problems at once: the regressive burden on delegators of small productive pools, and the structural disadvantage of the operators running them. Two findings from the operator-delegator-distribution mainnet analysis ([`../../diagnostic/sub-flows/operator-delegator-distribution/mainnet-analysis/README.md`](../../diagnostic/sub-flows/operator-delegator-distribution/mainnet-analysis/README.md)) are the CIP's direct targets.

| Finding | Why shared | Supporting observations |
| --- | --- | --- |
| **OPE.O1.F4 — The flat fee follows a $1/\sigma$ hyperbola: 47.5 % of pool reward at the sub-viable tier, 1.5 % at near-saturation** | The hyperbola is the mechanical consequence of a fixed-in-₳ levy on a size-proportional reward — exactly the regressivity a proportional margin floor rewrites out of the operator take. Maps to the CIP's explicit §3.3 delegator-yield claim. | OPE.O1.F1 (the flat fee delivers **60 %** of retail operator revenue — the passive channel dominates the active one) · OPE.O5.F1 (sub-viable delegators pay **18× more** for **0.30 pp** of extra yield — the delegator-side mirror of the hyperbola) |
| **OPE.O6.F1 — A sub-viable single-pool operator absorbs 48.3 % of pool rewards for 24,820 ₳/yr; an 11+ pool MPO absorbs 7.7 % for 1,035,496 ₳/yr — 42× more revenue at 6× less effective price** | The flat fee penalises small-pool delegators without compensating the operators running those pools. Redirecting take from the fixed channel to a proportional margin floor is the mechanical path toward §3.1 operator viability at small pool sizes — the CIP's implicit second claim. | OPE.O6.F4 (no single-pool operator in the retail market earns a competitive wage — the ~25,000 ₳/yr median covers infrastructure, not 5–15 hrs/month of skilled labour) |

### 1.4 Missing CPS — mapping to V2 §3.1

CIP-0023 ships without a formal Cardano Problem Statement. Read for what it actually points at, the CIP diagnoses a single mechanical defect: **return-on-stake is structurally unfair between small and large pools for the same delegator**. A delegator placing 100 k ADA in a 2 M pool keeps a far smaller share of that pool's reward than a delegator placing 100 k ADA in a saturated pool — because the flat `minPoolCost` is deducted *before* the margin, so it consumes a disproportionate share of a small pool's reward and only a trivial share of a saturated pool's. The instrument introduced to *ensure operator sustainability at the bottom of the distribution* has inverted its purpose at the delegator layer: it makes small pools **less attractive, not more**.

The reconstructed CPS statement therefore reads:

> **The operator growth path is not functioning as intended.** Small productive pools cannot accumulate the delegation they need to reach viability, because the protocol's own fixed-cost floor — intended to finance their operator — makes them strictly less attractive on net ROS than the large pools they are supposed to grow into. The instrument designed to support the small-pool layer has turned delegator mobility **against** the operators it was meant to protect.

This maps directly onto the foundational V2 specification — [§3.1 Guarantee operator viability across the entire productive population](../../README.md#31-guarantee-operator-viability-across-the-entire-productive-population):

| CPS slot | What V2 §3.1 provides — standing in for CIP-0023's missing CPS |
| --- | --- |
| Problem statement | The mechanism does not guarantee a delegation-mobility path from small productive pools to viable ones. The flat `minPoolCost` is paid out of the pool reward *before* the margin; at small pool sizes it dominates, making those pools uncompetitive on net ROS and stranding their operators below viability |
| Evidence base | OPE.O1.F4 ($1/\sigma$ hyperbola — floor at **47.5 %** of reward in sub-viable pools vs **1.5 %** at saturation) · OPE.O6.F1 (**42× more revenue at 6× less effective price** — operators who charge most earn least) · OPE.O6.F4 (no single-pool operator earns a competitive wage) |
| Specification | Any instrument that converts the fixed-in-₳ contribution into a size-proportional share, so ROS converges across pool sizes and delegators stop being mechanically steered away from small productive pools |
| Why foundational | V2 §3.1 is declared the **foundational specification** — every downstream problem (§3.3 delegator yield, §3.4 concentration, §4.1 pot sustainability) depends on a functioning growth path. If small pools cannot grow, the productive population hollows out from below |

**Why this is not "just a delegator problem".** The CIP's own rationale is written in delegator-facing language ("remove the regressive effect of `minPoolCost` on delegators"). V2 §3.1 frames it in operator-facing terms (viability, growth path). Both are correct — they are two views of the same asymmetry: the fixed-cost floor couples delegator mobility and operator viability through a single mechanical channel, and fails on both sides simultaneously.

## 2. Mechanism

**Formula** (floor applied during reward calculation, not at certificate level):

$$\mu^{\text{pool,effective}}_{i} = \max\left(\mu^{\text{pool}}_{i},\ \mu^{\text{pool}}_{\text{min}}\right)$$

**Design surface.**

| Property | Value |
| --- | --- |
| New parameter | `minPoolMargin` ($\mu^{\text{pool}}_{\text{min}}$) |
| Initial value | 0 (raised later via standard Parameter Change actions) |
| `minPoolCost` status | **Unchanged.** CIP text is explicit: "this proposal does not change or reduce the existing minimum fixed pool fee." |
| Re-registration | Not required — floor applied inside ledger reward calc |
| Layer | Fee (operator/member split, post-per-pool allocation) |
| Reward curve | Untouched |
| Pledge mechanics | Untouched |

**Intent.** Attack *fee regressivity*: `minPoolCost` alone imposes a disproportionately large effective fee rate on delegators to small pools, biasing delegation toward saturated pools. A variable-fee floor lets the effective operator take scale with pool stake.

### 2.1 Worked calibration from the CIP rationale

Comparison at epoch-382 parameters (rewards 31.2 M, saturation 64 M ADA), reference delegator 100 k ADA:

| Pool stake | Current (`minPoolCost = 340`, margin ≈ 0) | Proposed (`minPoolCost = 50`, `minPoolMargin = 1.5 %`) |
| ---: | ---: | ---: |
| 2 M | 22.7 % | 4.8 % |
| 5 M | 9.1 % | 2.8 % |
| 10 M | 4.5 % | 2.2 % |
| 20 M | 2.3 % | 1.8 % |
| 30 M | 1.5 % | 1.7 % |
| 64 M (sat.) | 0.7 % | 1.6 % |

Fee-rate dispersion collapses from **32× (22.7 / 0.7)** to **3× (4.8 / 1.6)**. **Caveat.** This requires *both* a reduced `minPoolCost` and the new margin floor; the margin floor alone does not flatten the small-pool regressive burden.

### 2.2 Updated calibration at current parameters (epoch ~623, 2026/04)

Two things have changed on mainnet since the CIP was drafted: `minPoolCost` was halved by governance to **170 ₳** (Conway-era action, DIA.1.3 floor), and the reward base has fallen with reserve depletion — hollow-pool yield at saturation is **~2.27 %/yr** (≈ 24 000 ₳/epoch at $z_0 \approx$ 77 M ₳). Re-running the CIP's own fee-rate comparison at today's values clarifies what the instrument still delivers after the halving already absorbed part of the fixed-fee relief.

Effective fee rate shown below is $\text{take}(\sigma) / R_{\text{pool}}(\sigma)$ (the share of each delegator's gross reward consumed by the operator take — independent of delegator stake). Per-pool reward below saturation approximated as $R(\sigma) \approx 24\,000 \times \sigma / 77\text{M}$ (pledge ≈ 0, hollow-pool base). Representative σ per tier follows the canonical nine-tier taxonomy from [pools-distribution §4.1.3](../../diagnostic/sub-flows/pools-distribution/mainnet-analysis/README.md#413-tier-definitions).

| Canonical tier | Stake range | Rep. σ | $R(\sigma)$ ₳/ep | (A) Today (`minPoolCost = 170`, margin ≈ 0) | (B) Standalone CIP-0023 (`minPoolCost = 170`, `minPoolMargin = 1.5 %`) | (C) Paired (`minPoolCost = 50`, `minPoolMargin = 1.5 %`) |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Zero-stake | 0 | 0 | 0 | — | — | — |
| Dormant | > 0 → ~100 K | 50 K | 15.6 | **100 %** | **100 %** | **100 %** |
| Sub-production | ~100 K → ~1 M | 500 K | 155.8 | **100 %** | **100 %** | **33.1 %** |
| Sub-viable | ~1 M → ~3 M | 2 M | 623 | 27.3 % | 28.4 % | 9.4 % |
| Healthy | ~3 M → ~38.5 M | 15 M | 4 675 | 3.64 % | 5.08 % | 2.55 % |
| Large healthy | ~38.5 M → ~61.6 M | 50 M | 15 584 | 1.09 % | 2.57 % | 1.82 % |
| Near-saturation | ~61.6 M → ~73.1 M | 67 M | 20 883 | 0.81 % | 2.30 % | 1.74 % |
| Saturated | ~73.1 M → ~80.8 M | 77 M | 24 000 | 0.71 % | 2.20 % | 1.71 % |
| Oversaturated | > ~80.8 M | 85 M | ≈ 24 000 | 0.71 % | 2.20 % | 1.71 % |

**Two regimes.** The table shows two structurally different populations:

- **Below-floor tiers (Dormant, Sub-production).** The pool reward does not reach `minPoolCost`; the operator take equals the entire pool reward and the delegator receives **nothing**. Under today's 170 ₳ floor and the standalone variant, these tiers are pinned at 100 %. The paired variant (`minPoolCost = 50`) partially relieves Sub-production (to 33 %) but leaves Dormant untouched.
- **Above-floor tiers (Sub-viable and up).** The margin floor begins to bite. This is the range where the CIP's 2021 dispersion analysis applies.

**Dispersion across the productive range** (Sub-viable → Saturated, excluding below-floor tiers which would inflate the ratios to infinity). (A) Today: **38×** (27.3 / 0.71) — *worse* than 2021's 32×, because yield fell faster than `minPoolCost`. (B) Standalone: **13×** (28.4 / 2.20) — partial relief at the top, floor still dominates at the bottom. (C) Paired: **5.5×** (9.4 / 1.71) — the only variant that collapses dispersion to a single-digit multiple.

> **Finding CIP-0023.S2.F1 [D] — The standalone variant partially flattens the delegator-side fee-rate hyperbola.** Productive-range dispersion compresses from **38× to 13×** — a partial delivery on the CIP's explicit §3.3 delegator-yield claim. The paired variant (out-of-scope for standalone) reaches 5.5×; CIP-0082 stage 2 reaches 1.00× (see [cip-0082.md §2.2](cip-0082.md#22-updated-calibration-at-current-parameters-epoch-623-202604)).

**Reading.** The halving of `minPoolCost` (340 → 170) delivered **zero** regressivity relief at the small-pool end — the ratio structure is unchanged because the yield denominator shrank in parallel. CIP-0023 *standalone* is stacked **on top of** the current 170 floor and only raises the effective rate at mid-to-large pools; it leaves the sub-viable tier essentially at its current 27 %. The paired variant (reduction of `minPoolCost` + margin floor) is the configuration that reproduces the CIP's 2021 intent — and it is explicitly out of scope for CIP-0023 standalone. In both variants, the two below-floor tiers (Dormant and, under standalone, Sub-production) remain **fully consumed by the fee** — the instrument does not help pools whose reward cannot reach the floor, regardless of margin calibration.

> **Finding CIP-0023.S3.F1 [B] — Below-floor tiers remain 100 % fee-consumed.** Dormant and Sub-production (pools whose epochal reward doesn't reach the 170 ₳ floor) are untouched by today's parameters and by the standalone variant — 100 % of pool reward absorbed by the flat fee. Even the out-of-scope paired variant relieves only Sub-production (100 % → 33 %) and leaves Dormant at 100 %. The instrument does not reach the struggling-pool tail it is nominally meant to help.

## 3. Limits of CIP-0023 as a standalone proposal

Two structural limits converge on the same verdict: CIP-0023 is better read as the historical seed of the margin-floor concept than as a live proposal to ratify today.

### 3.1 Functional redundancy — CIP-0082 stage 2 reproduces the same instrument

CIP-0082 stage 2 removes `minPoolCost` and introduces **`minPoolRate`** — mechanically identical to CIP-0023's `minPoolMargin`. The CIP-0082 author explicitly credits CIP-0023 for the concept. CIP-0082 then adds three further stages that reshape the fee layer end-to-end (drop of `minPoolCost`, rate progression, `k` increases), where CIP-0023 stops at the single instrument.

| Scenario | Outcome |
| --- | --- |
| CIP-0023 ships first | Stage 2 of CIP-0082 effectively renames and formalises `minPoolMargin` — `minPoolMargin` becomes a de-facto placeholder for `minPoolRate` |
| CIP-0082 ships first | CIP-0023 is strictly redundant — stage 2 already provides the margin floor, subsequent stages extend it |
| Both enacted as separate items | **Incoherent** — same instrument under two names, governance surface doubled, parameter calibration coupled across two CIPs |

**Implication.** Treating CIP-0023 and CIP-0082 as independent options forces a false choice — they are not independent. A coherent review cycle picks **CIP-0082** (which contains CIP-0023's instrument) and deprecates CIP-0023 as superseded (**CIP-0023.S1**).

> **Finding CIP-0023.S1.F1 [R] — Same instrument, two governance items.** CIP-0082 stage 2's `minPoolRate` is mechanically identical to CIP-0023's `minPoolMargin`, and the CIP-0082 author credits CIP-0023 explicitly. Ratifying both as separate items doubles the governance surface (two on-chain parameter changes, two calibration decisions, two review cycles) without delivering any additional mechanism — the CIP's core contribution (the margin-floor concept) is preserved intact inside CIP-0082 stage 2.

### 3.2 Partial coverage of the missing CPS — the instrument leaks revenue toward pools with no viability problem

The reconstructed CPS at §1.4 has two sides: ROS convergence across pool sizes (delegator layer) **and** income support for the operators of small pools that cannot yet attract delegation (operator layer). CIP-0023 addresses the first and actively mis-allocates the second: a proportional margin floor of 1.5 % produces a per-pool operator-revenue change that scales with pool size, so the same mechanism meant to help the small-pool tier **redirects a far larger ADA amount to operators that already have no viability problem** — and the mis-allocation is amplified when the same operator runs many pools.

Two projections follow, using the canonical pool-size taxonomy from the [pools-distribution tier definitions](../../diagnostic/sub-flows/pools-distribution/mainnet-analysis/README.md#413-tier-definitions) on the pool axis and the MPO bracket from the [operator-delegator profitability analysis](../../diagnostic/sub-flows/operator-delegator-distribution/mainnet-analysis/README.md#44-operator-profitability-versus-delegator-return) on the operator axis.

#### 3.2.1 Pool axis — Δ per pool across the nine canonical tiers

**Numbers at current parameters** (epoch ~623, 2026/04 — $R(\sigma) \approx 24\,000 \times \sigma / 77\text{M}$ ₳/epoch below saturation; reward capped at saturation cap $z_0 = 77$ M ₳; convention "declared margin ≈ 0" as in the CIP's own worked example). Representative σ values are taken at mid-tier.

| Canonical tier | Stake range | Rep. σ | $R_{\text{pool}}$ ₳/ep | Current op take | Standalone `minPoolMargin = 1.5 %` | **Δ per pool, std.** | Paired `minPoolCost = 50`, `minPoolMargin = 1.5 %` | **Δ per pool, paired** |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Zero-stake | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| Dormant | > 0 → ~100 K | 50 K | 15.6 | 15.6 | 15.6 | 0 | 15.6 | 0 |
| Sub-production | ~100 K → ~1 M | 500 K | 155.8 | 155.8 | 155.8 | 0 | 51.6 | **–104.2** |
| Sub-viable | ~1 M → ~3 M | 2 M | 623 | 170 | 176.8 | **+6.8** | 58.6 | **–111.4** |
| Healthy | ~3 M → ~38.5 M | 15 M | 4 675 | 170 | 237.6 | +67.6 | 119.4 | –50.6 |
| Large healthy | ~38.5 M → ~61.6 M | 50 M | 15 584 | 170 | 401.2 | +231.2 | 283.0 | +113.0 |
| Near-saturation | ~61.6 M → ~73.1 M | 67 M | 20 883 | 170 | 480.7 | +310.7 | 362.5 | +192.5 |
| Saturated | ~73.1 M → ~80.8 M | 77 M | 24 000 | 170 | 527.5 | **+357.5** | 409.3 | **+239.3** |
| Oversaturated | > ~80.8 M | 85 M | 24 000 | 170 | 527.5 | +357.5 | 409.3 | +239.3 |

*The row marked with bold Δ values at both ends is the headline.* Under the standalone variant, the per-pool operator-revenue uplift at **saturation is 52× the uplift at sub-viable** (357.5 / 6.8) and the Dormant / Sub-production tiers — where the pool reward cannot even reach the current floor — see **zero** transfer. Under the paired variant, the transfer is **inverted across the viability line**: Sub-production and Sub-viable operators lose 100–111 ₳/epoch each; Healthy operators still lose modestly; only Large-healthy and above are net recipients.

> **Finding CIP-0023.S2.F2 [R] — Per-pool Δ at Saturation is 52× the Δ at Sub-viable.** +357.5 vs +6.8 ₳/ep under the standalone variant — the instrument that the CIP's rationale introduces to *fund* small operators redirects the largest ADA transfer to pools that had no viability problem to begin with. The Dormant and Sub-production tiers receive zero transfer (pool reward doesn't reach the 170 ₳ floor, so the 1.5 % margin applies to zero).

#### 3.2.2 Operator axis — Δ per entity by n-MPO bracket

Per-pool Δ stacks over the operator's fleet. Each MPO bracket below uses its **mean pools per entity** and **mean pool stake** from the [operator-delegator profitability analysis](../../diagnostic/sub-flows/operator-delegator-distribution/mainnet-analysis/README.md#44-operator-profitability-versus-delegator-return) (epoch 623, retail hollow segment; 73 epochs/yr). Sub-populations inside hollow-single-pool are listed separately because the mean would otherwise hide the split at the viability line.

| n-MPO bracket | Entities | Pools/entity | Mean stake/pool | $R_{\text{pool}}$ ₳/ep | **Δ per entity per year — standalone** | **Δ per entity per year — paired** |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Single-pool — Sub-viable (< 3 M) | 155 | 1 | 1.81 M | 564 | **+431 ₳/yr** | **–8 198 ₳/yr** |
| Single-pool — Healthy (3–38.5 M) | 214 | 1 | 11.40 M | 3 554 | +3 706 ₳/yr | –4 920 ₳/yr |
| Single-pool — Large healthy (38.5–62 M) | 29 | 1 | 50.69 M | 15 798 | +17 114 ₳/yr | +8 483 ₳/yr |
| Single-pool — Near-saturation (62–77 M) | 16 | 1 | 69.38 M | 21 623 | +23 491 ₳/yr | +14 863 ₳/yr |
| 2-pool MPO | 17 | 2.00 | 38.24 M | 11 918 | +25 725 ₳/yr | +8 468 ₳/yr |
| 3–5 pool MPO | 24 | 3.92 | 29.47 M | 9 183 | +38 693 ₳/yr | +4 865 ₳/yr |
| 6–10 pool MPO | 9 | 7.44 | 35.37 M | 11 024 | +88 423 ₳/yr | +24 223 ₳/yr |
| 11+ pool MPO | 7 | 19.29 | 33.41 M | 10 413 | **+216 298 ₳/yr** | **+49 852 ₳/yr** |

The ratio that matters is not between tiers of a single pool but between the operator CIP-0023 claims to help and the operator it hands the largest transfer to. Under the **standalone** variant, an 11+ pool MPO entity gains **502×** the annual uplift of a sub-viable single-pool operator (216 298 / 431). Under the **paired** variant, every MPO bracket is a net winner and every sub-viable single-pool operator is a net loser, inverting the transfer across the viability line on the entity axis as well.

> **Finding CIP-0023.S2.F3 [R] — On the operator-fleet axis, the transfer compounds to a 502× gap.** 11+ pool MPO entities gain **+216 298 ₳/yr** under the standalone variant; sub-viable single-pool operators gain **+431 ₳/yr**. The pool-axis 52× amplification (S2.F2) and the fleet-axis 9.6× amplification stack multiplicatively — the reform is regressive on the operator layer, mirroring the delegator-layer regressivity it is meant to correct.

#### 3.2.3 Reading — why this fails the missing CPS

**Standalone variant** (`minPoolCost` unchanged at 170, margin floor 1.5 %):

- The small-pool delegator effective rate barely moves (§2.2: 27.3 % → 28.4 %). Delegators of the pools the CIP is supposed to help see **no meaningful ROS improvement**.
- The mechanism is a **delegator-to-operator transfer, not a small-pool-to-large-pool redistribution reversal** — and inside that transfer, large operators accumulate the largest share because (a) Δ per pool grows with σ (pool axis), and (b) MPO entities multiply Δ across their fleet (operator axis). The two amplifications compound: **502×** gap between the 11+ pool MPO entity and the sub-viable single-pool operator.
- Sub-production and Dormant pools — the struggling-pool tail the reform gestures at — receive **zero** uplift, because their pool reward does not reach the unchanged 170 ₳ floor.

**Paired variant** (`minPoolCost = 50`, margin floor 1.5 %):

- Solves the delegator-side ROS problem: fee-rate dispersion collapses from 38× to 5.5× (§2.2).
- But the operator-side path is **inverted across the viability line**: sub-viable and viable-but-small operators lose **50–112 ₳/epoch** per pool; Large-healthy and above gain **113–239 ₳/epoch**. On the entity axis, every MPO bracket is a net recipient and every sub-viable single-pool operator is a net contributor (**–8 198 ₳/yr** at $0.25/ADA ≈ $2 050 of annual income stripped from an operator who already cannot cover the cost floor).
- The operators the reconstructed CPS asks to be supported are **funding the reform**; the operators already at viability are **paid by it**.

**Why this matters against the missing CPS.** §1.4 identifies "the operator growth path is not functioning as intended" as the CIP's foundational claim. A reform that (i) fails to materially improve ROS at the small-pool end under its standalone shape, (ii) under its paired shape actively reduces small-operator revenue while raising large-operator revenue, and (iii) in both shapes amplifies the transfer **up the MPO ladder** via fleet multiplication, is **not a growth-path instrument**. It is a fee-schedule redistribution whose monotonicity on both the pool axis and the operator-fleet axis is the opposite of what the CPS requires (**CIP-0023.S2**) — and that is before composition with the redundancy of §3.1 (**CIP-0023.S1**). The reform also misses the struggling-pool tail entirely, since the below-floor tiers stay 100 %-fee-consumed under every variant (**CIP-0023.S3**).

### 3.3 References

- **Missing-CPS anchor:** §1.4 of this file; V2 [§3.1 Guarantee operator viability across the entire productive population](../../README.md#31-guarantee-operator-viability-across-the-entire-productive-population).
- **Numerical baseline:** §2.2 of this file (per-pool reward derivation at $z_0 = 77$ M ₳, saturated-pool reward ≈ 24 000 ₳/ep, `minPoolCost = 170 ₳`).
- **Diagnostic findings:** [`../../diagnostic/sub-flows/operator-delegator-distribution/mainnet-analysis/README.md`](../../diagnostic/sub-flows/operator-delegator-distribution/mainnet-analysis/README.md) — OPE.O1.F4 ($1/\sigma$ hyperbola), OPE.O6.F1 (42× revenue at 6× price), OPE.O6.F4 (no competitive wage for single-pool operators).
- **Successor CIP:** [`cip-0082.md`](cip-0082.md) — stage 2 absorbs the margin-floor mechanism; stages 1, 3, 4 extend the reform to the full fee layer.
- **Canonical source:** CIP-0023 official page at [cips.cardano.org/cip/CIP-0023](https://cips.cardano.org/cip/CIP-0023) (identity and history in §1.1).

> **Status:** Active 2026/04/23. Fee-layer candidate. CIP identity and sources in §1; evaluation references in §3.3.
